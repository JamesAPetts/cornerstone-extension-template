function initializeMixin() {
  /*
  Initialises a mixin when it as attached to a tool.
  */
}

/**
 * @mixin templateMixin Reusable functionality.
 * Say you have similar input loops, see e.g. CircleSegmentationMixin in cornerstoneTools.
 * @memberof Mixins
 */
export default {
  // e.g.:
  // postTouchStartCallback = (evt) => {},
  // postMouseDownCallback = (evt) => {},
  // mouseClickCallback = (evt) => {},
  // touchDragCallback = (evt) => {},
  // mouseDragCallback = (evt) => {},
  // mouseMoveCallback = (evt) => {},
  // touchEndCallback = (evt) => {},
  // mouseUpCallback = (evt) => {},
  // renderToolData = (evt) => {},
  initializeMixin
};
